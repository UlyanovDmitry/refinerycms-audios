<%= render 'refinery/admin/search_header', :url => refinery.audios_admin_audios_path %>
<div class='pagination_container'>
  <% if @audios.any? %>
    <%= render 'audios' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
